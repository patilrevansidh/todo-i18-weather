import { makeStyles } from "@material-ui/core";

const styles = makeStyles((theme) => ({
  container: {
      padding: '2rem',
      margin: '2rem'
  },
  action:{
    marginTop: '2rem'
  },
  heading:{
    marginBottom: '2rem'
  },
  textField:{
    marginTop: '1rem'
  },
  leftMargin:{
    marginLeft: '1rem'
  }
}));

export default styles;
